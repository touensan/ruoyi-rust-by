# RuoYi-Rust BY 协作约定

开始工作先阅读 `ai_codex.md`、`docs/DEVELOPMENT.md` 和 `docs/FEATURES.md`。用中文及时更新重要决策、开发里程碑、逻辑原理、真实验证结果和遗留问题。记忆文件是可复核的交接记录，不能把计划写成已完成事实。

默认在主线程按最小充分原则工作；未经用户要求不使用子代理。前端尽量保持 RuoYi-Go BY 的布局和行为，后端必须为 Rust，数据库为 MySQL。不要引入 PHP/Go 服务依赖。提交真实凭据、数据库备份、私人域名或业务数据被禁止。

新增 API 必须在服务端校验权限；用户数据范围要在 SQL 分页前以及详情/修改/删除/导出时执行。角色、菜单、部门变更及角色分配只允许超级管理员。所有 SQL 值绑定参数，动态标识符仅允许来自内置表定义或经过验证的数据库元数据。不要给未实现功能返回成功。

发布前核对许可证、依赖风险、Rust 测试、MySQL API 集成测试、前端构建和浏览器验收。编译前检查资源，使用独立目录，禁止占用生产数据库做测试。该项目公开发布不等于获得替换其他生产站点的授权。

系列架构必须遵循 `docs/ARCHITECTURE.md`：单一后台、同一账号与登录、RBAC 动态控制菜单/路由/页面/接口；`admin` 单向继承 `common` 的功能权限。管理员同时可访问普通用户功能，不建立独立用户端或以非管理员条件排除管理员。保留超级管理员边界，功能权限继承不自动放宽数据范围。

功能变更必须同步 `docs/VERSIONS.md`、README、功能范围和开发记忆；明确 main、历史标签、下载包的区别。旧 Release 说明标明所属版本，不能把历史未实现项当作当前状态；未实现接口不得返回操作成功。
