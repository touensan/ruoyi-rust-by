# 安全说明

请在 GitHub 仓库启用的私密漏洞报告渠道中报告可利用问题，不要在公开 Issue 粘贴真实凭据或数据。若私密渠道尚未开启，请先提交不含利用细节的联系请求。

v0.1.0 是预发布版本；用于正式业务前应根据部署环境完成审查和回归。

## 2026-09-05 依赖检查

通过 OSV 批量查询 Cargo.lock 中的依赖，发现 SQLx MySQL 传递依赖 `rsa 0.9.10` 对应 [RUSTSEC-2023-0071](https://rustsec.org/advisories/RUSTSEC-2023-0071.html)。上游公告列为 RSA 私钥操作的时序侧信道问题，尚无已发布补丁。

本项目不提供 RSA 私钥解密/签名接口，登录使用 bcrypt、配置使用 AES-GCM。`rsa` 由 MySQL 驱动引入；这不代表该依赖已修复。部署应使用受保护的数据库连接，并继续跟踪 SQLx/RustCrypto 的修复版本。

前端生产依赖审计零报告；包含开发工具链的完整 npm audit 仍报告 7 项 high、2 项 moderate，涉及 Vite、SVG 构建插件及其传递依赖。开发服务器仅绑定回环地址，发布包不包含 Node 依赖；升级构建插件仍是后续维护任务。详细结果及执行日期见 `docs/VALIDATION.md`。锁定依赖不能替代持续更新；发布前应重新检查。

## 当前边界

- 管理接口必须置于 HTTPS 后；不要把数据库或临时测试服务暴露到公网。
- 限速与验证码当前为单实例实现，服务重启会重置；多实例部署需共享状态。
- 富文本公告由具有公告维护权限的受信任人员创建。站点 head 代码不允许注入脚本。
- 超级管理员可以修改权限和敏感设置，应保护其账号。任何新业务模块都必须额外定义自己的权限和数据范围。
- 不记录密码、完整令牌、配置密钥或原始请求体到操作日志；不要在运行日志中开启外部代理的敏感请求体采集。
